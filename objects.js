var playlist = new object({key: '0'})

function updatePlaylist(playlist, artist name, songTitle) {
  playist(artistName) = songTitle
  return playlist
}

function removeFromPlaylist(playlist, artistName) {
  delete playlist[artistName]
  return playlist
}
